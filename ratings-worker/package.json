{
  "name": "gravelleestrie-ratings-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 execute gravelleestrie-ratings --local --file=./schema.sql",
    "db:migrate:remote": "wrangler d1 execute gravelleestrie-ratings --remote --file=./schema.sql",
    "test": "node --test test/*.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
